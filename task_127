    function findShort(s) {

        let arr = s.split(' ')
        let a = arr[0].length;
        for (let i = 0; i < arr.length; i++) {
            if (arr[i].length < a) {
                a = arr[i].length
            }
        }
        return a
    }
